{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install fastapi uvicorn pydantic groq pymongo bcrypt python-dotenv ddgs langdetect deep-translator"
  },
  "deploy": {
    "startCommand": "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
  }
}