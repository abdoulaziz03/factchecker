[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install fastapi uvicorn pydantic groq pymongo bcrypt python-dotenv ddgs langdetect deep-translator"]

[start]
cmd = "python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"